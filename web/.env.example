# Copy to web/.env and fill from your Supabase project
# (Project Settings → API). The key here is the PUBLIC publishable/anon key —
# never put the service_role / secret key in the client.
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_KEY=YOUR-PUBLISHABLE-OR-ANON-KEY
